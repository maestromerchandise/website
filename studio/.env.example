# Copy to .env and fill in by hand. Never commit .env.
# Both values are public identifiers, not secrets, but they still belong here
# rather than hardcoded in sanity.config.ts.

SANITY_STUDIO_PROJECT_ID=your_sanity_project_id_here
SANITY_STUDIO_DATASET=production
